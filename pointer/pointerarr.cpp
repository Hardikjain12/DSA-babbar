#include <iostream>
using namespace std;
int main()
{

    int arr[10] = {23, 122, 41, 67};

    //  cout <<" address of first memory block is " << arr << endl;
    //  cout << arr[0] << endl;
    //  cout <<" address of first memory block is " << &arr[0] << endl;

    //  cout << "value of 0th index " << *arr << endl;
    //  cout << "5th " << *arr + 1 << endl; (*arr = 23 value then +1 so the value of *arr + 1 = 24)
    //  cout << "Value at 1 index  " << *(arr + 1) << endl;
    //  cout << "value of 0 index  " << *(arr) + 1 << endl;
    //  cout << "8th " << arr[2] << endl;
    //  cout << "value at 2nd index  " << *(arr+2) << endl;

    //  int i = 3;
    //  cout <<"Value at 3rd index "<< i[arr] << endl;

    // int tem[10] = {1, 2};
    // cout << sizeof(tem) << endl;
    // cout << " 1st " << sizeof(*tem) << endl;
    // cout << " 2nd " << sizeof(&tem) << endl;

    // int *ptr = &temp[0];
    // cout << sizeof(ptr) << endl;
    // cout << sizeof(*ptr) << endl;
    // cout << sizeof(&ptr) << endl;

    // int a[20] = {1,2,3,5};
    // cout  << "address of 0th index " <<&a[0] <<endl;
    // cout  << "address of 0th index " <<&a <<endl;
    // cout  << "address of 0th index " <<a <<endl;

    // int * p = &a[0];
    // cout <<"Address of 0th index " << p <<endl;
    // cout  <<"value at 0 index "<< *p <<endl;
    // cout  <<"address of p "<< &p <<endl;
    
    
    return 0;
}